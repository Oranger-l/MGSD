# Modality Gap Text Cases

These files show the exact text-only user prompts after replacing `<TEXT-STATE>` with the environment text state.

They are previews only. The evaluation pipeline under `evaluate/ckpt_eval` is not modified.
